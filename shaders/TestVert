#version 420 core

layout(location = 0) in vec3 in_pos;

uniform mat4 viewMatrix;
uniform mat4 projectionMatrix;
uniform mat4 transformationMatrix;

void main()
{
	mat4 tvpMatrix = projectionMatrix * transformationMatrix * viewMatrix;
	gl_Position = tvpMatrix * vec4(in_pos,1);
}